while(!q.empty()) {
                //     int x = q.front().first;
                //     int y = q.front().second;
                //     for(int k=0; k<4; k++){
                //         int nx = x + dx[k];
                //         int ny = y + dy[k];
                //         q.pop();
                //         if(nx <0 || ny < 0 || nx >= n || ny >= m) continue;
                //         if(visited[nx][ny]) continue;

                //         q.push({nx, ny});
                //         sum++;
                //         visited[nx][ny] = true;
                //     }
                // }