while(!q.empty()) {
    //     int x = q.front().first.first;
    //     int y = q.front().first.second;
    //     int block = q.front().second;

    //     q.pop();

    //     if(x == n && y == m) {
    //         if(answer > block) {
    //             answer = block;
    //             continue;
    //         }
    //     }

    //     for(int i=0; i<4; i++) {

    //     }
    // }